# P2P Web Messenger

Децентрализованный веб-мессенджер с peer-to-peer архитектурой, использующий WebRTC для прямого соединения между пользователями.

## Особенности

- 🔐 Peer-to-peer связь без центрального сервера для сообщений
- 🌍 Работает между пользователями из разных стран
- 🎲 Автоматическая генерация случайного username
- 🔍 Поиск пользователей по username
- 💾 Сохранение истории сообщений локально
- 📱 Адаптивный дизайн

## Технологии

- **Frontend**: HTML5, CSS3, Vanilla JavaScript (ES6+)
- **Backend**: Node.js, Express, WebSocket (ws)
- **P2P**: WebRTC Data Channels
- **Testing**: Vitest, fast-check

## Установка

```bash
# Установить зависимости
npm install
```

## Запуск

### Локальная разработка

```bash
# Запустить signaling server
npm run server

# Открыть client/index.html в браузере
```

### Тестирование

```bash
# Запустить все тесты
npm test

# Запустить тесты в watch режиме
npm run test:watch
```

## Структура проекта

```
p2p-web-messenger/
├── client/              # Клиентская часть
│   ├── index.html
│   ├── styles/         # CSS файлы
│   ├── js/             # JavaScript модули
│   └── assets/         # Иконки и изображения
├── server/             # Signaling server
│   ├── index.js
│   └── SignalingServer.js
├── tests/              # Тесты
│   ├── unit/
│   └── property/
└── package.json
```

## Развертывание

### Шаг 1: Развертывание Signaling Server

#### Вариант A: Railway (Рекомендуется)

1. Создайте аккаунт на [Railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит настройки из `railway.toml`
5. После развертывания скопируйте URL сервера (например: `https://your-app.railway.app`)

#### Вариант B: Render

1. Создайте аккаунт на [Render.com](https://render.com)
2. Нажмите "New +" → "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: `cd server && npm install`
   - Start Command: `cd server && npm start`
5. Скопируйте URL после развертывания

#### Вариант C: Glitch

1. Перейдите на [Glitch.com](https://glitch.com)
2. Создайте новый проект
3. Импортируйте код из GitHub
4. Glitch автоматически запустит сервер
5. Скопируйте URL проекта

### Шаг 2: Обновление конфигурации клиента

Откройте `client/js/config.js` и обновите URL signaling сервера:

```javascript
export const SIGNALING_SERVER_URL = isProduction
  ? 'wss://your-server-url.railway.app' // Замените на ваш URL
  : 'ws://localhost:8080';
```

### Шаг 3: Развертывание Client

#### Вариант A: Vercel (Рекомендуется)

1. Установите Vercel CLI: `npm i -g vercel`
2. Запустите: `vercel`
3. Следуйте инструкциям
4. Vercel автоматически использует настройки из `vercel.json`

#### Вариант B: Netlify

1. Установите Netlify CLI: `npm i -g netlify-cli`
2. Запустите: `netlify deploy`
3. Для production: `netlify deploy --prod`
4. Netlify использует настройки из `netlify.toml`

#### Вариант C: GitHub Pages

1. Скопируйте содержимое папки `client` в корень репозитория
2. Перейдите в Settings → Pages
3. Выберите branch и папку
4. GitHub Pages опубликует сайт

### Шаг 4: Тестирование

1. Откройте URL клиентского приложения в браузере
2. Вам будет присвоен случайный username
3. Откройте приложение в другой вкладке или на другом устройстве
4. Используйте поиск, чтобы найти другого пользователя
5. Начните общение!

## Как это работает

1. Пользователь открывает приложение и получает случайный username
2. Signaling server координирует установление WebRTC соединений
3. После установления соединения, сообщения передаются напрямую между пользователями
4. История сообщений сохраняется локально в браузере

## Лицензия

MIT
